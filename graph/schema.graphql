schema {
  query: Query
  mutation: Mutation
}

type Mutation {
    test(in: String): OperationStatus
}

type Query {
    school(schoolID : String!):[Department!]!

}


type OperationStatus {
    succeeded: Boolean!
}
